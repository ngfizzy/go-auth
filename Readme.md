# Go Auth

A simple authentication service written in Golang

## Endpoints

```text
PORT: 8000

POST /api/register
    - body 
        name, email, password
POST /api/login
    - body
        email, password
GET /api/user
POST /ap/logout
```
